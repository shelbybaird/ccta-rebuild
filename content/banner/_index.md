---
title: "Alert banner"
# This section produces no page of its own, and the cascade below gives the
# banner file the same treatment its own front matter used to carry. It lives
# here because it is a fact about how the site is structured, not something an
# officer edits — and a key the editing tool does not know about would be
# dropped the first time the banner was saved through it.
build:
  render: never
  list: never
cascade:
  build:
    render: never
    list: local
---
