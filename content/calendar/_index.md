---
title: "Calendar"
weight: 20
# This page publishes twice from one set of content: as the page a reader looks
# at, and as `calendar.ics`, the feed a calendar application subscribes to.
# Naming the second output here rather than in the site configuration keeps it
# on the one page that has anything to put in it.
outputs: ["html", "calendar"]
---
