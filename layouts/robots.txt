{{- if eq (printf "%v" site.Params.indexable) "true" -}}
User-agent: *
Allow: /

Sitemap: {{ "sitemap.xml" | absURL }}
{{- else -}}
# This build is a preview, not the Association's public site. Search engines are
# asked to stay out so that unfinished pages are never indexed. The pages also
# carry a noindex tag, which is the control that actually keeps an address out
# of a search index; this file only asks crawlers not to fetch them.
User-agent: *
Disallow: /
{{- end -}}
